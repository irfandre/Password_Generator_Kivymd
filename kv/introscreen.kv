#:import STANDARD_INCREMENT kivymd.material_resources.STANDARD_INCREMENT
#:import get_hex_from_color kivy.utils.get_hex_from_color
#:import get_color_from_hex kivy.utils.get_color_from_hex
<IntroScreen>:
#    canvas:
#        Color:
#            rgba: 0, 0, 1, 1    # Blue
#
#        # size and position of Canvas
#        Rectangle:
##            pos: self.pos
#            size: root.height
#            y: dp(100)
    Image:
        id: big_pic
        source: 'data/orange.png'
        size_hint: None, None
        size: root.width + dp(200), root.height + dp(50)
        pos_hint: {'center_x': .5}
        allow_stretch: True
        keep_ratio: False
        y: dp(200)


    MDBoxLayout:
        id: box
        orientation: 'vertical'
        adaptive_size: True
        pos_hint: {'center_x':.5}
        y: big_pic.y + self.height + dp(250)
#        md_bg_color: 1,0.2,1,1
        MDLabel:
            id: title
            text: 'Generator'
            adaptive_size: True
            opposite_colors: True
#            color: get_color_from_hex('F49C8A')
            color: (1,1,1,1)
            font_style: 'H4'
            bold: True
            adaptive_height: True


        MDLabel:
            text: 'Tecnsec'
            halign: 'right'
            font_style: 'Button'
            adaptive_height: True
#            y: la.y + self.height
#    Image:
#        source: 'small.png'
#        size_hint: None, None
#        size: STANDARD_INCREMENT, STANDARD_INCREMENT
#        x: box.x
#        y: box.y + self.height
    Image:
        id: grid
        source: 'data/chemical.png'
#        allow_stretch: True
#        keep_ratio: False
#        size_hint: None, None
#        size: STANDARD_INCREMENT, STANDARD_INCREMENT
#        x: big_pic.x
        y: box.y - big_pic.y

#    MDIconButton:
#        icon: "android"
#        theme_text_color: "Custom"
#        text_color: app.theme_cls.primary_color

    MDRaisedButton:
        id: start_button
        text: "[color=#ffffff]Start[/color]"
        adaptive_height: True
#        x: box.x
        bold: True
#        md_bg_color: get_color_from_hex('F49C8A')
#        md_bg_color: rgba(253, 0, 253,255)
#        color: 1,1,1,1
#        text_color: 1, 1, 1, 1
        halign: 'center'
        pos_hint: {'center_x':0.5}
#        md_bg_color: get_color_from_hex('F49C8A')
#        md_bg_color: app.theme_cls.primary_light

#        text_color : rgba(253, 0, 253,255)
#        size;
#        size_hint: 0.7,None
        y: big_pic.y -  self.height
#        on_release: root.parent.current = 'generator_screen'
        on_release: app.screen.current = 'generator_screen'
        bg_hint_color: app.theme_cls.primary_light

    MDLabel:
        id: hi_text
        text: "HI!"
        halign: 'center'
        adaptive_size: True
        pos_hint: {'center_x':0.5}
        color: app.theme_cls.primary_color if app.theme_cls.theme_style=='Light' else [1,1,1,1]
        font_style: 'H5'
#        y: big_pic.y - box.y
        y: start_button.y + dp(65)
    MDLabel:
#        id:
        text: "Welcome to the password generator app"
        halign: 'center'
        adaptive_size: True
        pos_hint: {'center_x':0.5}
#        color: app.theme_cls.disabled_hint_text_color if app.theme_cls.theme_style=='Light' else [1,1,1,1]
#        on_color: app.the_cls.disabled_hint_text_color
        color: get_color_from_hex('313356')
        font_style: 'Subtitle2'
#        y: big_pic.y - box.y
        y: hi_text.y - self.height
#        y: self.height - dp(200)

#        size_hint: None, None
##        size: STANDARD_INCREMENT , STANDARD_INCREMENT
#        x: box.x
#        y: bu.y - self.height
#        adaptive_height: True
##        pos_hint: {'center_x':.5}
#    MDLabel:
#        id: two
#        text: 'two'
#        size_hint: None, None
##        size: STANDARD_INCREMENT , STANDARD_INCREMENT
#        x: box.x
#        y: one.y -self.height
#        adaptive_height: True
#
#    MDLabel:
#        text: 'two'
#        size_hint: None, None
##        size: STANDARD_INCREMENT , STANDARD_INCREMENT
#        x: box.x
#        y: two.y - self.height
#        adaptive_height: True

#    MDLabel:
#        text: 'twosd sldj  sdlj f'
#        size_hint: None, None
##        size: STANDARD_INCREMENT , STANDARD_INCREMENT
#        x: box.x
#        y: big_pic.y - self.height - one.height
#        adaptive_height: True

#    MDLabel:
#        text: 'twsdfssd sdjf jsldj  sdj lsdjl s sdljf ddj k'
##        size_hint: None , None
##        size: STANDARD_INCREMENT , STANDARD_INCREMENT
#        halign: 'center'
#        y: big_pic.y - self.height - one.height
#        adaptive_height: True

